# SportsGuard AI — Python Dependencies
# Install: pip install -r requirements.txt
# Python >= 3.10 required.

# ── Web Framework ───────────────────────────────────────────────────────────
flask>=3.0                            # REST API backend (Module E)
flask-cors>=4.0                       # Cross-origin support for frontend

# ── Gemini AI (google-genai SDK) ────────────────────────────────────────────
google-genai>=1.0.0                   # Current Gemini SDK — genai.Client() (Module C)
pydantic>=2.0.0                       # Strict JSON schema enforcement

# ── Computer Vision / Video Processing ──────────────────────────────────────
opencv-python>=4.9.0                  # Frame extraction from MP4/MOV (Module A)

# ── Image Processing & Hashing ──────────────────────────────────────────────
Pillow>=10.0.0                        # PIL Image — shared by all modules
imagehash>=4.3.1                      # Perceptual hashing (Module A)

# ── Numerical ───────────────────────────────────────────────────────────────
numpy>=1.24.0                         # Frame array manipulation

# ── C++ Extension Build ────────────────────────────────────────────────────
pybind11>=2.11                        # C++ matcher compilation (Module B)

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0                           # Test runner (Module G)
